# Optional — point this at your backend to sync with MongoDB via server-test.
# Leave unset to run entirely on local AsyncStorage data.
EXPO_PUBLIC_API_URL=https://your-codespace-name-3001.app.github.dev
